"use client";
import styled from "styled-components";

const StyledHeader = styled.header`
  height: 30px;
  background-color: var(--color-primary);
`;

const Logo = styled.a`
  display: flex;
  align-items: center;
  text-decoration: none;
`;
export default function LayoutHeader() {
  return <StyledHeader>ㅇㅇ</StyledHeader>;
}
